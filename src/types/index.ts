export type Client = {
  id: string;
  name: string;
  email: string;
  address: string;
  phone: string;
  gstNumber: string;
  postalCode: string;
  [key: string]: string;
};

export type Company = {
  id: string;
  name: string;
  email: string;
  address: string;
  phone: string;
  gstNumber: string;
  postalCode: string;
  [key: string]: string;
};
